import { render, screen } from '@testing-library/react';
import App from '../App';

describe("Tests de la racine + récupération json par fetch", () => {
  it("récupération du json par fetch", () => {

  });
})
